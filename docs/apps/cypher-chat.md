# Cypher Chat

Status: `ready`

Source route: direct Waveshare AMOLED build from sibling `cypher-chat`.

## Waveshare Controls

- Uses the existing visual/touch profile from `cypher-chat-firmware`.
- Serial fallback remains available at `115200`.
- Launcher return is available through the compatibility return helper.

## SD Data

No launcher-managed runtime assets are required.

## Port Notes

Built with `BOARD_PROFILE_WAVESHARE_TOUCH_AMOLED_18` and the shared 16 MB
Waveshare launcher partition layout.
