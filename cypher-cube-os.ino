// Intentionally code-free. Arduino's sketch preprocessor can inject a
// recursive setup() call when the launcher entrypoint lives in an .ino file.
// The real setup()/loop() definitions live in src/ArduinoEntrypoint.cpp.
